export type SellerSnapshot={seller:string;observedAt:string;active:boolean;availableQuantity?:number;price?:number};
export function detectStockout(snapshots:SellerSnapshot[]){const bySeller=new Map<string,SellerSnapshot[]>();for(const s of snapshots){const a=bySeller.get(s.seller)??[];a.push(s);bySeller.set(s.seller,a)}const events=[];for(const [seller,rows] of bySeller){rows.sort((a,b)=>a.observedAt.localeCompare(b.observedAt));const prior=rows.slice(0,-1).some(r=>r.active);const latest=rows.at(-1);if(prior&&latest&&!latest.active)events.push({seller,outOfStockAt:latest.observedAt,signal:"SUPPLY_GAP" as const});}return events;}
