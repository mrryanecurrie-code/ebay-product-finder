export function nextAction(input:{decision:"BUY"|"WATCH"|"REJECT";recommendedQty:number;shippingVerified:boolean;historyVerified:boolean;supplierUrl?:string}){if(input.decision==="REJECT")return "Do not source this product.";if(!input.historyVerified)return "Verify eBay.com historical sold demand before purchasing.";if(!input.shippingVerified)return "Get the supplier shipping quote and enter it to finalize landed cost.";if(input.decision==="WATCH")return "Watch the product; do not buy yet.";return `Source ${input.recommendedQty} unit${input.recommendedQty===1?"":"s"} for the initial eBay.com test${input.supplierUrl?` from ${input.supplierUrl}`:""}.`;}
